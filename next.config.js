/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            'images.pexels.com',
            'dummyimage.com'
        ]
    }
};

export default nextConfig;

